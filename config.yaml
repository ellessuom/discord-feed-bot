discord:
  webhook_url: '${DISCORD_WEBHOOK_URL}'

sources:
  - id: 'steam_730'
    type: 'steam_game'
    name: 'Counter-Strike 2'
    appid: 730
    enabled: true

settings:
  max_posts_per_run: 10
  include_images: true
  post_order: 'newest_first'
